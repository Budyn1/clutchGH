import React from 'react';

const Sklep = ({}) => {
  return (
    class Flywheel {
      constructor(model, waga, rozmiar, compatibility) {
        this.model = model
        this.waga = waga
        this.rozmiar = rozmiar
        this.compatibility = compatibility
      }
    }

    class ClutchPlate {
      constructor(rozmiar, material, maksTorque, compatibility) {
        this.rozmiar = rozmiar
        this.material = material
        this.maksTorque = maksTorque
        this.compatibility = compatibility
      }
    }

    class PressurePlate {
      constructor(stan, model, maksTorque, compatibility) {
        this.stan = stan
        this.model = model
        this.maksTorque = maksTorque
        this.compatibility = compatibility
      }
    }

    let tarczaKevlarova = new ClutchPlate("240mm", "kevlar", "480nm", "m57")
  );
}

export default Sklep;
